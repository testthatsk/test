name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: "*/5 * * * *"

jobs:
  build:
    permissions: write-all  
    uses: testthatsk/.github/.github/workflows/ci-java-testing.yml@main


